{
  "permissions": {
    "allow": ["Bash(npx vitest run *)"]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npx vitest run --reporter=json --silent=passed-only 2>/dev/null | jq '[.testResults[].assertionResults[] | select(.status == \"failed\")]' | grep -v '^\\[\\]$' || true",
            "statusMessage": "Running tests...",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
